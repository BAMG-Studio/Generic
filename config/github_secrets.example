# ForgeTrace GitHub Actions Secrets Template
# Copy these values into GitHub → Settings → Secrets and variables → Actions.
# Replace every placeholder before saving. Never commit actual secrets.

AWS_ACCESS_KEY_ID=REPLACE_WITH_terraform_output_-raw_aws_access_key_id
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_terraform_output_-raw_aws_secret_access_key
AWS_DEFAULT_REGION=us-east-1
DVC_REMOTE_BUCKET=forgetrace-models-<env>-<suffix>
GITHUB_TOKEN=ghp_your_token_with_repo_workflow_scopes
MLFLOW_TRACKING_URI=http://mlflow.internal:5050
MLFLOW_USERNAME=mlflow_admin
MLFLOW_PASSWORD=change_me_every_180_days

# Optional integrations
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
SENTRY_DSN=https://public_key@o0.ingest.sentry.io/0
PREFECT_API_KEY=prefect-api-key-if-used
